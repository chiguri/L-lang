言語Lのインタープリタ

ある本で使われていた疑似言語のインタープリタのSchemeによる実装。
R5RS準拠ならば間違いなく動くはず。

一応実行前に簡単な構文検査と型検査を行う。
また、(f# X 10)などで呼び出している部分は(sharp f X 10)のように関数をsharpとして呼び出すことにしている。
構文解析をマジメにやるのが面倒だったから。

パラメータを与えるとXからではなくX1から始まるようにしている。

callマクロを導入している。

処理中の環境が出力されるdisplay関数を導入している（デバッグ用）

(execute)で標準入力から開始、(execute-file "filename")でファイルから読み込んで開始。

疑問はコードから。
